package com.eahm.learn.business.interactors.common

class See {
    /**
     * interactors contain packages that represent the fragments
     * in the application. Depending on which fragment implement
     * a certain usecase then the package will contain that usecase.
     *
     * common contain usecases that are present in 2 or more fragments.
     * That way we dont have to assign the use case to a particular
     * fragment.
     */
}